// Assignment # 3
// Storing a famous quote and its author

let famousquote: string= "If everything around seems dark, look again, you may be the light."

let author : string= "Rumi"

// print a quote with its author name

console.log(`${author} said, "${famousquote}"`);